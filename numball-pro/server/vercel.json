{
  "version": 2,
  "installCommand": "cd .. && npm install -g pnpm && pnpm install && cd shared && pnpm run build && cd ../server && npx prisma generate",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
